# Core dependencies for French SSML cascade models
torch>=2.0.0
transformers>=4.36.0
peft>=0.7.0
accelerate>=0.25.0
safetensors>=0.4.0
sentencepiece>=0.1.99

# Optional: for better performance
# bitsandbytes>=0.41.0  # For quantization support
